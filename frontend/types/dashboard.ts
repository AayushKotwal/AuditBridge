export interface DashboardStats {
  total_controls: number;
  total_evidence: number;
  pending_evidence: number;
  approved_evidence: number;
  total_audit_requests: number;
  open_findings: number;
  closed_findings: number;
} 